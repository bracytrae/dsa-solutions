# Dynamic Arrays 

dynamic arrays - can grow/shrink and store additional elements. 

## Dynamic Array Insertion 

-- the next empty space is found and element is inserted there because it's dynamic and does not contain a fixed size 

## Resize 
> why double the capacity?
<br>

> other operations
<br>






doubling the size of an array is so important if we wish to increase the size of a array that is dynamic 