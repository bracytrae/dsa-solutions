# Dynamic Array Problems

Dynamic arrays can grow when new values are added. In Python, the built-in
`list` type provides dynamic-array behavior.

## Solutions

- [Concatenation of Array](concatenation-of-array/Solution.py)
